import { Link } from 'react-router-dom';

const SecondLogo = () => {
  return (
    <Link to={'/'} className=' flex items-center justify-center text-[#d9d749] text-4xl'>
      {'{'}
    </Link>
  );
};

export default SecondLogo;
